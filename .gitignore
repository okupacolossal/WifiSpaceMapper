# ESP-IDF build output (regenerated by idf.py)
build/
sdkconfig.old
managed_components/
dependencies.lock

# Wi-Fi credentials — real values live in main/secrets.h, never committed
main/secrets.h

# Local machine setup (downloaded USB drivers etc. — not part of the firmware)
_setup/

# Editor / OS
*.pyc
__pycache__/
.DS_Store
