// Template for Wi-Fi credentials. COPY this file to secrets.h and fill in your
// real 2.4 GHz network. secrets.h is gitignored, so your password never gets
// committed to the public repo. secrets.example.h (this file) IS committed.
#pragma once

#define WIFI_SSID "your-2.4GHz-network-name"
#define WIFI_PASS "your-password"
